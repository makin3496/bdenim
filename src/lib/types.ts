// Re-export everything from strapi.ts
// Types are now defined directly in strapi.ts for Strapi v5 compatibility
export type {
  Homepage,
  Service,
  BlogPost,
  SiteSettings,
  StrapiMedia,
} from "./strapi";
